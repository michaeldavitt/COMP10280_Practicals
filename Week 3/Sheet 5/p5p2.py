# p5p2 - Program checks whether a number is non-negative or not

user_num = int(input("Please input an integer: "))

if user_num >= 0:
    print(user_num, "is non-negative")

else:
    print(user_num, "is negative")

print("Finished")
